source /var/db/repos/lto-overlay/sys-config/ltoize/files/make.conf.lto
CFLAGS="${CFLAGS} ${COMMON_FLAGS}"
CXXFLAGS="${CFLAGS}"
DISTDIR="/var/cache/distfiles"
FCFLAGS="${CFLAGS}"
FFLAGS="${CFLAGS}"
PORTDIR="/var/db/repos/gentoo"
USE="-berkdb -browser-integration -cramfs -crda -desktop-portal -dhclient -discover -display-manager -doc -dvbpsi -eme-free -gdbm -gnutls -gtk-doc -gusb -handbook -hangouts -initramfs -legacy-systray -libnotify -mix -mysql -nio-buffers -passwdqc -playlist -plymouth -ppp -pulseaudio -pwhich -rsync-verify -ruby_targets_ruby23 -seccomp -sendmail -sftp -ssp -suid -system-bootstrap -tcmalloc -trash -usb_stream -vanilla -webrtc-aec -webstart -wext -wpe -zlib"